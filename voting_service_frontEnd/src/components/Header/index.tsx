import { HeaderWrapper, Logo, LogoutButton } from './styles'

const Header = () => {
  return (
    <HeaderWrapper>
      <Logo>Voting System</Logo>
      <LogoutButton>Sair</LogoutButton>
    </HeaderWrapper>
  )
}

export default Header
